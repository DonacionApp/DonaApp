<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cuenta Verificada</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background-color: #f5f5f7;
            padding: 40px 20px;
        }

        .email-container {
            max-width: 540px;
            margin: 0 auto;
            background: #ffffff;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
        }

        .content {
            padding: 48px 40px;
        }

        .icon {
            width: 64px;
            height: 64px;
            background: linear-gradient(135deg, #34C759 0%, #30D158 100%);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 32px;
            font-size: 32px;
            animation: scaleIn 0.5s ease-out;
        }

        @keyframes scaleIn {
            from {
                transform: scale(0);
            }
            to {
                transform: scale(1);
            }
        }

        .title {
            font-size: 24px;
            color: #1d1d1f;
            margin-bottom: 12px;
            font-weight: 600;
            text-align: center;
        }

        .greeting {
            font-size: 17px;
            color: #1d1d1f;
            margin-bottom: 24px;
            font-weight: 500;
        }

        .message-box {
            color: #1d1d1f;
            font-size: 15px;
            line-height: 1.7;
            padding: 24px;
            background: #f5f5f7;
            border-radius: 10px;
            margin: 24px 0;
            white-space: pre-line;
        }

        .success-message {
            text-align: center;
            color: #34C759;
            font-size: 15px;
            font-weight: 500;
            margin: 24px 0;
        }

        .info-box {
            background: #f0f9ff;
            border-left: 3px solid #0071e3;
            border-radius: 8px;
            padding: 20px;
            margin: 24px 0;
        }

        .info-box p {
            color: #1d1d1f;
            font-size: 14px;
            line-height: 1.6;
            margin-bottom: 8px;
        }

        .info-box p:last-child {
            margin-bottom: 0;
        }

        .button {
            display: block;
            width: 100%;
            padding: 16px;
            background: #000000;
            color: #ffffff;
            text-decoration: none;
            border-radius: 10px;
            font-weight: 500;
            font-size: 15px;
            text-align: center;
            margin: 32px 0;
            transition: background 0.2s;
        }

        .button:hover {
            background: #333333;
        }

        .footer {
            padding: 32px 40px;
            text-align: center;
            border-top: 1px solid #f5f5f7;
        }

        .footer p {
            color: #86868b;
            font-size: 13px;
            line-height: 1.6;
        }

        @media only screen and (max-width: 600px) {
            body {
                padding: 20px 10px;
            }

            .content {
                padding: 40px 24px;
            }

            .footer {
                padding: 32px 24px;
            }

            .message-box {
                padding: 20px;
            }
        }
    </style>
</head>

<body>
    <div class="email-container">
        <div class="content">
            <div class="icon">✓</div>
            
            <h1 class="title">{{title}}</h1>
            
            <p class="greeting">Hola {{user}},</p>
            
            <p class="success-message">Tu cuenta ha sido procesada exitosamente</p>
            
            {{#if message}}
            <div class="message-box">{{message}}</div>
            {{/if}}

            <div class="info-box">
                <p><strong>✓</strong> Tu correo electrónico ha sido confirmado</p>
                <p><strong>✓</strong> Tu cuenta está completamente activa</p>
                <p><strong>✓</strong> Ya puedes acceder a todas las funcionalidades</p>
            </div>

            {{#if url}}
            <a href="{{url}}" class="button">Comenzar</a>
            {{/if}}
        </div>

        <div class="footer">
            <p>¡Bienvenido a nuestra plataforma!</p>
            <p>Si tienes alguna pregunta, no dudes en contactarnos.</p>
        </div>
    </div>
</body>

</html>